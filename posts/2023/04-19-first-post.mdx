---
title: 'My First Blog Post'
description: 'How to create a Next.js TypeScript blog site with MDX.'
slug: 'first-post'
date: '2023-04-19'
---

# My First Blog Post

Welcome to my first blog post using MDX in a Next.js TypeScript blog site!

Here's an example of some TypeScript code:

```tsx
import React from 'react'

interface MyComponentProps {
  message: string
}

const MyComponent: React.FC<MyComponentProps> = ({ message }) => {
  return <div>{message}</div>
}

export default MyComponent
```
